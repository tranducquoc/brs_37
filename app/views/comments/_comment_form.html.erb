<%= form_for @comment, remote: true do |f| %>
  <div class="field">
  <div><%= f.hidden_field :review_id, value: review.id %></div>
    <%= f.text_area :content, placeholder: t("comment_this_review") %>
  </div>
  <%= f.submit t("reply")%>
<% end %>
